manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: ziembski
      url-base: https://github.com/Ziembski
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: main   # DIAGNOSTIC: v0.3(最新稳定)USB 断连路径疑似硬崩;main=Zephyr 4.x 全新 UDC 栈
      import: app/west.yml
    # DIAGNOSTIC: peahen 模块临时移除(锁 v0.3 时代,main 兼容性未知,减少变量)
  self:
    path: config
