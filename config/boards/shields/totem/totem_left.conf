# ── 拔线崩溃诊断第三轮:零 CDC 构建 ─────────────────────────────────────
# 假说:所有崩溃构建都带 USB CDC(studio-rpc-usb-uart / zmk-usb-logging),
# Zephyr 3.5 nRF USB 设备栈在 CDC 断开路径上硬崩。本构建无任何 CDC:
# 无 snippet、无 USB 日志、无 Studio。若拔线存活 → CDC 定罪。
CONFIG_ZMK_STUDIO=n
