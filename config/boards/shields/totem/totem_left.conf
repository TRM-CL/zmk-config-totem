# ── 拔线崩溃诊断(分支 diagnostic/unplug-crash,临时)──────────────────────
# 目的:捕获 USB 拔出瞬间的内核/BT/端点日志。现象:拔线 → 全静默(广播停、
# 按键死、3V3 稳)→ 插线复活。怀疑 v0.3 在 USB unplug 事件路径上硬崩。
CONFIG_ZMK_USB_LOGGING=y

# 与 zmk-usb-logging snippet 的 CDC 节点冲突(zmk#3031),诊断期间临时关闭
CONFIG_ZMK_STUDIO=n
