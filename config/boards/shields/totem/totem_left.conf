# ── ZMK main(Zephyr 4.x UDC 栈)拔线审判构建 ─────────────────────────────
# 目的:若新 USB 栈在「USB 拔出→3秒死」上存活,则定罪 v0.3/Zephyr 3.5 老栈。
# 带串口日志:观察新栈的断连处理路径。
CONFIG_ZMK_USB_LOGGING=y

# 与 zmk-usb-logging snippet 的 CDC 节点冲突(zmk#3031),诊断期间临时关闭
CONFIG_ZMK_STUDIO=n
