include:
  - board: seeeduino_xiao_ble
    shield: totem_left
    # 诊断临时:换 zmk-usb-logging snippet 引出 USB 串口日志(电量标定用),
    # 平时用 studio-rpc-usb-uart。两者 CDC 节点冲突,不可同时开(zmk#3031)。
    snippet: zmk-usb-logging
  - board: seeeduino_xiao_ble
    shield: totem_right
  - board: seeeduino_xiao_ble
    shield: settings_reset
